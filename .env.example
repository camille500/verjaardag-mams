# AWS Configuration
# Credentials are automatically provided by AWS Amplify via IAM role
# For local development, use AWS CLI: aws configure
REGION=eu-central-1

# DynamoDB
DYNAMODB_TABLE_NAME=party-rsvps

# SES (Simple Email Service)
# Make sure this email/domain is verified in SES
SES_FROM_EMAIL=noreply@yourdomain.com
